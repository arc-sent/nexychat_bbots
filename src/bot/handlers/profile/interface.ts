export interface UserPUT {
    name?: string,
    age?: number,
    gender?: boolean,
    searchGender?: boolean,
}

export interface EditStatistick {
    coinViewed?: boolean,
    ratingViewed?: boolean
}